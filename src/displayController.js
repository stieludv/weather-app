// get data from weatherData and display
// render all components


const displayController = (() => {

})();



const components = (() => {

    // I can put weatherCards in carousel

    // currentWeatherCard will contain most info for todays data only

    // background will render appropriate color/gradient/image for current conditions

    // header will contain words "weather app"

    return {
        background,
        weatherCard,
        currentWeatherCard,
        header,
    }
})();